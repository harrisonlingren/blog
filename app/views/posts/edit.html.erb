<h1>Edit post</h1>

<%= render 'form' %>


<td><%= link_to 'Delete', post_path(@post), method: :delete, data: {confirm: 'Are you sure?'}, :class => "btn btn-default" %></td>

<%= link_to 'Back', posts_path, :class => "btn btn-default" %>
